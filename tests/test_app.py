def test_app() -> None:
    """Exists to allow pytest to run through withou throwing an error
    message.

    TODO(sebastianfuehr): Remove, once other tests are implemented.
    """
    assert True
